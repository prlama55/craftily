import { Meta, Story, Canvas, Controls } from '@storybook/blocks';
import * as RadioStories from './Radio.stories';

<Meta of={RadioStories} />

# Radio

The Radio component allows users to select a single option from a set of choices.

## Basic Usage

Basic radio button group:

<Canvas>
  <Story of={RadioStories.Basic} />
</Canvas>

## Sizes

Radio buttons are available in different sizes:

<Canvas>
  <Story of={RadioStories.Sizes} />
</Canvas>

## States

Different states of radio buttons:

<Canvas>
  <Story of={RadioStories.States} />
</Canvas>

## Interactive Controls

Use the controls below to experiment with the radio button's properties:

<Controls />
