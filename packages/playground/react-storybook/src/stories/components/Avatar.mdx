import { Meta, Story, Canvas, Controls } from '@storybook/blocks';
import * as AvatarStories from './Avatar.stories';

<Meta of={AvatarStories} />

# Avatar

The Avatar component is used to represent users with initials or images. It supports different sizes and can display both images and text.

## Sizes

Avatars are available in different sizes:

<Canvas>
  <Story of={AvatarStories.Sizes} />
</Canvas>

## With Image

Avatars can display user images:

<Canvas>
  <Story of={AvatarStories.WithImage} />
</Canvas>

## With Initials

When no image is available, avatars can display user initials:

<Canvas>
  <Story of={AvatarStories.WithInitials} />
</Canvas>

## Interactive Controls

Use the controls below to experiment with the avatar's properties:

<Controls />
